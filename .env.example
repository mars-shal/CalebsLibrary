# Convex — client-safe values only.
# Copy to .env and fill in. Secret keys (deploy key, admin passphrase) never belong here —
# set those via `npx convex env set` so they only live server-side.
VITE_CONVEX_URL=https://your-project-name-0000.convex.cloud
