# Supabase — client-safe values only (anon key is designed for public clients).
# Copy to .env and fill in. Secret keys (service_role etc.) never belong here.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
