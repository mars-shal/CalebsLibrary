# Mobile env — copy to `.env` (gitignored) and fill in. Never commit secrets.
# Client-safe values only (EXPO_PUBLIC_* ships in the app bundle).
EXPO_PUBLIC_CONVEX_URL=https://your-project-name-0000.convex.cloud
# Sentry DSN lives in EAS secrets, not here.
